VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# NOTE: Encryption key is stored in Supabase Vault (server-side only).
# It is NOT needed in the client environment.
# Configure it via Supabase Dashboard → Settings → Vault.
